@{
    Layout = "_Layout";
}
<main class="container">
    <form id="editSurveyForm">
        <div class="form-group">
            <label for="surveyName">Nombre de la encuesta</label>
            <input type="text" id="surveyName" name="surveyName" value="Encuesta de Satisfacción" required />
        </div>

      @*   <div id="questionsContainer">

            <div class="form-group question-item">
                <label for="question1">Pregunta 1</label>
                <input type="text" id="question1" name="questions[]" value="¿Qué tan satisfecho estás con el servicio?" required />
            </div>

            <div class="form-group question-item">
                <label for="question2">Pregunta 2</label>
                <input type="text" id="question2" name="questions[]" value="¿Cómo calificarías la atención recibida?" required />
            </div>
        </div>
 *@
        <div id="questionsContainer">
            <div class="form-group question-item">
                <div class="question-input-wrapper">
                    <div class="text-input-group">
                        <label for="question1">Pregunta:</label>
                        <input type="text" id="question1" value="¿Qué tan satisfecho estás con el servicio?" name="questions[]" required />
                    </div>
                    <div class="number-input-group">
                        <label for="questionNumber1">Número:</label>
                        <input type="text" id="questionNumber1" name="questionNumbers[]" value="1" />
                    </div>
                </div>
            </div>
        </div>
        <div class="button-group">
            <button type="button" class="btn-add" onclick="addQuestion()">Agregar otra pregunta</button>
        </div>

        <div class="button-group">
            <button type="submit" class="submit-button">Guardar cambios</button>
            <button type="button" class="cancel-button" onclick="cancelar()">Cancelar</button>
        </div>
    </form>
</main>

 <script>
      let questionCount = 1; // Ya hay 1 cargada
    function addQuestion() {
      if (questionCount >= 10) {
        alert("Solo se permiten hasta 10 preguntas.");
        return;
      }
      questionCount++;
      const container = document.getElementById("questionsContainer");
      const div = document.createElement("div");
      div.className = "form-group question-item";
      div.innerHTML = `
         <div class="question-input-wrapper">
            <div class="text-input-group">
                <label for="question">Pregunta:</label>
                <input type="text" id="question${questionCount}" name="questions[]" required />
            </div>
            <div class="number-input-group">
                <label for="questionNumber${questionCount}">Número:</label>
                <input type="text" id="questionNumber${questionCount}" name="questionNumbers[]" value="${questionCount}" required />
            </div>
        </div>
      `;
      container.appendChild(div);
    }

    function cancelar() {
      if (confirm("¿Deseas cancelar la edición?")) {
        window.history.back();
      }
    }
 </script>